---
title: 使用官方云镜像创建Debian 12 KVM 客户机
date: 2025-05-18 11:20:50
tags:
  - KVM
categories:
  - - GNU/Linux
---

使用debian官方云镜像安装debian 12 KVM客户机
<!-- more -->

0. 安装软件

```bash
$ sudo apt install virtinst libvirt-daemon-system libvirt-clients cloud-utils
```

1. 下载官方镜像

```bash
$ sudo mkdir /var/lib/libvirt/images/templates
$ wget https://cdimage.debian.org/images/cloud/bookworm/latest/debian-12-nocloud-amd64.qcow2
$ sudo mv debian-12-nocloud-amd64.qcow2 /var/lib/libvirt/images/templates/
```

2. 增加镜像文件系统空间

假设客户机标识为guest_name
```bash
$ sudo mkdir /var/lib/libvirt/images/guest_name
$ sudo cp /var/lib/libvirt/images/templates/debian-12-nocloud-amd64.qcow2 /var/lib/libvirt/images/guest_name/root-disk.qcow2
$ sudo qemu-img resize /var/lib/libvirt/images/guest_name/root-disk.qcow2 100G
```

3. 生成云镜像初始化iso

cloud-init.cfg 为云镜像初始化文件，可以配置很多选项

```bash
$ sudo echo "#cloud-config
hostname: guest_name
" | sudo tee /var/lib/libvirt/images/guest_name/cloud-init.cfg

$ sudo cloud-localds \
  /var/lib/libvirt/images/guest_name/cloud-init.iso \
  /var/lib/libvirt/images/guest_name/cloud-init.cfg
```

5. 创建客户机

```bash
$ sudo virsh net-start default
$ sudo virsh net-autostart default
```
先启动客户机默认网络并设置为自动启动，不然创建客户机时会报错误：
```
ERROR    Requested operation is not valid: network 'default' is not active
```
如果使用桥接网络不需要启动默认网络

```bash
$ sudo virt-install \
  --name guest_name \
  --memory 2048 \
  --disk /var/lib/libvirt/images/guest_name/root-disk.qcow2,device=disk,bus=virtio \
  --disk /var/lib/libvirt/images/guest_name/cloud-init.iso,device=cdrom \
  --os-variant debian11 \
  --virt-type kvm \
  --graphics none \
  --network bridge=br0,model=virtio \
  --import
```

出现登录控制台后输入root进入系统，默认没有设置密码，退出控制台键入```ctrl + ]```

重新连接控制台
```bash
$ sudo virsh list --all
 Id   Name          State
-----------------------------
 1    guest_name   running

$ sudo virsh console 1
```

管理客户机
```bash
$ sudo virsh shutdown guest_name
$ sudo virsh start guest_name
```

6. 网络配置

客户机创建后可以通过virsh edit来修改客户机的网络及其他配置

```bash
$ sudo virsh edit guest_name
```

如果安装时使用的是默认网络，可以从默认网络
```bash
<interface type='network'>
<mac ***/>
<source network='default'/>
<address ***/>
</interface>
```
更改为桥接网络
```bash
<interface type='bridge'>
<mac ***/>
<source bridge='br0'/>
<address ***/>
</interface>
```

更改网络之前先安装上
```bash
$ sudo apt install openvswitch-switch
```

不然更改网络配置文件后`netplan apply`时会报错误：

```Cannot call openvswitch: ovsdb-server.service is not running.```


从debian 12 "Bookworm" 开始，云镜像默认网络配置工具为netplan
修改网络配置文件`/etc/netplan/90-default.yaml`

```bash
network:
    version: 2
    ethernets:
        enp1s0:
          dhcp4: no
          addresses: [192.168.0.16/24]
          gateway4: 192.168.0.1
          nameservers:
            addresses: [114.114.114.114]
```

然后执行`# netplan apply`

会有
```bash
`gateway4` has been deprecated, use default routes instead.
See the 'Default routes' section of the documentation for more details.
```
的过时告警，所以改用route来配置默认网关

```bash
network:
    version: 2
    ethernets:
        enp1s0:
          dhcp4: no
          dhcp6: no
          addresses: 
            - 192.168.0.16/24
          routes:
            - to: default
              via: 192.168.0.1
          nameservers:
            addresses: [114.114.114.114]
```

7. 清理

```bash
$ sudo rm /var/lib/libvirt/images/guest_name/cloud-init.iso 
$ sudo rm /var/lib/libvirt/images/guest_name/cloud-init.cfg
```

References:

[1] [KVM](https://wiki.debian.org/KVM)

[2][libvirt](https://wiki.archlinux.org/title/Libvirt)

[3][Netplan](https://wiki.debian.org/Netplan)

[4][NetworkConfiguration](https://wiki.debian.org/NetworkConfiguration)

[5][ 虚拟化技术之kvm虚拟机创建工具virt-install
](https://www.cnblogs.com/qiuhom-1874/p/13520939.html)

[6][/etc/netplan/network-manager-all.yaml 配置服务器ip](https://www.cnblogs.com/zjdxr-up/p/17444844.html)

[7][KVM 通过virsh console连入虚拟机](https://www.cnblogs.com/xieshengsen/p/6215168.html)

[8][KVM虚拟机常用的一些命令](https://www.cnblogs.com/ariesblog/p/13808670.html)